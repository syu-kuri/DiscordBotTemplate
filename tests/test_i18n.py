# TODO(Codex): Implement per DESIGN.md section 8 ("Testing / CI").
# - Verify that en.yml and ja.yml have exactly the same set of keys
# - Verify t() never raises, even with an unknown key or an unknown locale
