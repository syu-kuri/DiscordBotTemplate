DISCORD_TOKEN=
COMMAND_PREFIX=!
OWNER_IDS=
LOG_LEVEL=INFO
CONFIG_PATH=config/config.yaml
# Set this during development to sync slash commands instantly to a single guild
# (leave unset for a global sync, which can take up to an hour to propagate)
DEV_GUILD_ID=
