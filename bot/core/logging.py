# TODO(Codex): Implement per DESIGN.md section 7 ("Logging").
# - Console output honoring LOG_LEVEL
# - RotatingFileHandler writing to logs/bot.log (~5MB x 3 backups)
# - Attach discord.py's own "discord" logger to the same handlers
# - Log messages are always in English regardless of locale.default (see DESIGN.md 3.3)
