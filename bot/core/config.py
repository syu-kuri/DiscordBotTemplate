# TODO(Codex): Implement per DESIGN.md section 3 ("Configuration").
# - Load .env via python-dotenv
# - Load the YAML at CONFIG_PATH, merging in config.example.yaml's keys as
#   defaults, and expose it as an immutable settings object (dataclass recommended)
# - Missing keys must fall back to their default values
