# TODO(Codex): Implement per DESIGN.md section 6 ("Utilities").
# - Provide consistently styled Embed factory functions such as
#   success_embed(title, description), error_embed(...), info_embed(...)
# - Callers are expected to pass already-translated strings (via bot.core.i18n.t())
#   for title/description — embeds.py itself doesn't need to know about i18n keys
