# TODO(Codex): Implement per DESIGN.md section 6 ("Utilities").
# - is_owner(): an app_commands.check that only allows users listed in OWNER_IDS
# - has_permissions(**perms): raise a shared permission-error exception on failure
#   (so error_handler.py can catch it)
