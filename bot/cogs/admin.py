# TODO(Codex): Implement per DESIGN.md section 5 ("Cog Specification") for admin.py.
# - /reload <cog>, /sync, /shutdown
# - Restrict execution to users listed in OWNER_IDS (utils.checks.is_owner)
# - Do not hardcode strings/descriptions — fetch them via bot.core.i18n.t("admin.*")
