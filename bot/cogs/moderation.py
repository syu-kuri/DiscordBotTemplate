# TODO(Codex): Implement per DESIGN.md section 5 ("Cog Specification") for moderation.py.
# - /kick, /ban, /timeout, /clear
# - Gate execution permissions using the decorators in utils.checks
# - Do not hardcode strings/descriptions — fetch them via bot.core.i18n.t("moderation.*")
